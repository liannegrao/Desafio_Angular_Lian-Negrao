{
  "name": "desafio-angular-lian-negrao",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run backend\" \"npm run frontend\" \"npm run json-server\"",
    "start": "cd frontend && npm start",
    "build": "cd frontend && npm run build --prod",
    "backend": "cd backend && npm run backend",
    "frontend": "cd frontend && npx ng serve --port 4200",
    "json-server": "cd backend && npm run json-server"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
