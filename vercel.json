{
  "version": 2,
  "builds": [
    {
      "src": "frontend/package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "dist/angular"
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/dist/angular/$1"
    }
  ]
}
